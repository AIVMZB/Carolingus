SEED: 42
MARGIN: 2
MARGIN_THRESHOLD: 1
EMBED_DIM: 32
IMG_SIZE: 120
IMG_FORMAT: RGB
DATASET_PATH: "datasets/word_dataset"
SAVE_PATH: "runs/syntax_embedding"
PRETRAINED:

SIMPLE_TRAIN:
  STEPS_PER_EPOCH: 400
  BATCH_SIZE: 8
  LR: 0.00005
  EPOCHS: 80

HARD_TRAIN:
  STEPS_PER_EPOCH: 400
  BATCH_SIZE: 8
  LR: 0.0000005
  EPOCHS: 30
  STR_DIST_THRESHOLD: 1
